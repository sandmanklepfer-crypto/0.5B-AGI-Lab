<div align="center">

# 🧠 0.5B-AGI-Lab

### 半个十亿参数的脑子，能干到什么程度？

**我们用 479 个实验，把它的边界一寸一寸量了出来。**

[![Params](https://img.shields.io/badge/params-0.5B-blueviolet)]()
[![Experiments](https://img.shields.io/badge/experiments-479-orange)]()
[![Findings](https://img.shields.io/badge/hard_findings-6-red)]()
[![No Training](https://img.shields.io/badge/no_training-needed-success)]()

</div>

---

## 宣言

> **所有人都在说「参数不够」。**
> **我们花 479 次实验去问：到底哪一步不够？**
>
> **答案不是「参数不够」，是「形式不够」。**

---

## 🔥 六个硬结论（全可复现）

### ① 免训练抹除拒绝行为

```python
bias = -100 * (W_token_embd @ d_refusal)   # 写进 output.bias，几十秒完成
```

- **因果验证**：反向 α=+150 **无效** → 证明是真机制，不是噪声
- 算术与知识能力**完全保留**
- 不需要训练、不需要 GPU

### ② 12 步长链，全对

```
[ 1] diff(sin(x)*cos(x), x) -> cos(2*x)       ✅
[ 2] diff(cos(2*x), x)      -> -2*sin(2*x)    ✅
...                    连续 12 步 12/12
```

**关键**：分次调用成功，一次给长历史失败。
→ **瓶颈是「单次前向深度」，不是「记忆容量」。**

### ③ 它的失败点，精确到工序

```
算对：The derivative of -1024cos(2x) is -2048sin(2x)   ✅
然后：...which simplifies to -4096*sin(2x)             ❌ 自己多做了一步
```

**加一句「禁止化简」→ 立刻正确。**

> **它不是不会，是总要多做一道错的工序。**

### ④ 真正的瓶颈：**形式库**

```
数列 [2, 7, 20, 57, 166]

只有「封闭公式」形式  →  ❌ 搜不到
加上「递归」形式      →  ✅ a_n = 3a_(n-1) − 2n + 5
                          预测 [491, 1464]  全对
```

**同一个搜索器，同一个数列。只多给一种形式，就通了。**

### ⑤ 模型 vs 搜索器（同一批题）

| 数列 | 搜索器 + 验证器 | 0.5B 模型 |
|---|---|---|
| `[1,4,9,16,25]` | ✅ `n²` | ❌「前两项之和」|
| `[3,8,15,24,35]` | ✅ `n²+2n` | ❌「前一项减1」|
| `[2,4,8,16,32]` | ✅ `2ⁿ` | ❌「前两项之和」|
| `[1,2,5,14,42]` | ✅ Catalan | ❌「前两项之和」|

**模型 0/5 —— 而且 5 次都用同一个模板。**

### ⑥ 给足记忆也救不了

```
给完整 11 步历史 → 仍答错（把第10阶系数当成第12阶）
→ 瓶颈不是记忆
```

---

## 🎯 现在，我们卡在哪（**这就是要你来的地方**）

**0.5B 已经能：** 触发工具调用 / 走长链 / 被抹除拒绝
**0.5B 还不能：** 自己算 / 反事实 / 原创算法

**而唯一真正的瓶颈，是「形式库」。**

### 形式库共建清单

| 形式 | 状态 | 需要 |
|---|---|---|
| 封闭公式（多项式/指数） | ✅ 已收录 | — |
| **递归/差分方程** | ✅ 已验证有效 | — |
| 线性方程组 | ⬜ **空缺** | ~50 行 + 1 个测试 |
| 微分方程 | ⬜ **空缺** | ~50 行 + 1 个测试 |
| 积分变换 | ⬜ **空缺** | ~50 行 + 1 个测试 |
| 矩阵/线性代数 | ⬜ **空缺** | ~50 行 + 1 个测试 |
| 图/组合 | ⬜ **空缺** | ~50 行 + 1 个测试 |
| 概率/统计 | ⬜ **空缺** | ~50 行 + 1 个测试 |

> **每补一种形式，能解决的整个问题域就打开。**
> **欢迎 PR —— 这是最直接能推动项目的事。**

---

## 📐 诚实的边界表（欢迎打脸，但请带数据）

| 能做 | 做不到 |
|---|---|
| ✅ 工具调用触发（5 格式 100%） | ❌ 自己的计算（`2x+1` → 0/10）|
| ✅ 长链执行（12 步，需严格格式）| ❌ 反事实推理（只复读题目）|
| ✅ 免训练去对齐（α=100）| ❌ 原创算法设计（无限循环）|
| ✅ 形式库搜索（加形式即命中）| ❌ 跨形式类比 |
| ✅ 近义格式泛化 | ❌ 语义转化（应用题）|

---

## 🚀 三个可以立刻上手的入口

**入门（1小时）**
```bash
git clone https://github.com/sandmanklepfer-crypto/0.5B-AGI-Lab
python src/library_grow.py      # 看形式库怎么自己长大
python src/topo.py              # 看耦合拓扑的影响
```

**进阶（1天）**
```
docs/02_权重手术.md   → 复现免训练去对齐
docs/03_长链协议.md   → 复现 12 步长链
```

**贡献（最需要）**
```
补一种「形式」到 src/form_library.py
加自己的边界测试到 bench/
```

---

## 📂 目录

```
docs/          4 份实测结论（能力边界/权重手术/长链协议/形式库）
src/           48 个核心源码
experiments/   479 个探索脚本 + 34 份资产文档（含明确负结果）
```

---

## 🔬 最新资产：桥流重铸 V170（2026-09-15）

**权重化开 → 架桥 → 顺桥重流。权重形状/参数量不变，只有谱分布变了。**

| ALPHA | 单矩阵偏离 | 困惑度 | 倍数 | 判定 |
|---|---|---|---|---|
| 0.003 | 0.0031 | 8.2498 | 1.00x | ✅ 几乎无损 |
| **0.01** | **0.0102** | **8.3828** | **1.02x** | ✅ 语义完好 |
| 0.03 | 0.0301 | 10.2065 | 1.24x | ✅ 语义保持 |
| 0.10 | 0.0949 | 18.5667 | 2.25x | ❌ 崩 |
| 0.35 | 0.2743 | 605.7147 | 73.45x | ❌ 崩 |

基线困惑度 = **8.2471** | 桥维 = **250**（与独立估算的 0.5B 有效容量完全吻合）

**核心**：只重排奇异值 `S`，`U` 和 `Vᵀ` 完全不动 → 几何保住 → 语义不乱。
**代价**：预算很窄，单矩阵偏离必须 **≤1~3%**。
**详情**：`experiments/资产_桥流重铸_v170.md`

## 📜 License

代码 Apache-2.0 ｜ 文档 CC-BY-4.0
无模型权重（请从 [Qwen 官方](https://huggingface.co/Qwen) 获取）

## 🤝 推广计划

- [ ] 补齐 6 种空缺形式
- [ ] 建立「0.5B 边界排行榜」（欢迎提交你的测试）
- [ ] 跨模型对比（0.5B / 1.5B / 3B / 7B 在同一套形式库下）
- [ ] 形式库标准协议（让更多人接入）

**想参与？开个 Issue 说说你想做哪块。**

---

<div align="center">

**如果你也相信「小模型 + 好形式」这条路 —— 给个 ⭐**

</div>
